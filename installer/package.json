{
  "name": "claude-config-installer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --compile src/index.ts --outfile ../install"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
